import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Chỉnh sửa gói thiết bị',
}

export default function Page({ children }: { children: React.ReactNode }) {
  return children
}
